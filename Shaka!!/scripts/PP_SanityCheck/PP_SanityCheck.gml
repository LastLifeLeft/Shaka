if ((PP_DISPLAY_TARGET_HEIGHT > PP_WINDOW_MIN_HEIGHT) || (PP_DISPLAY_TARGET_WIDTH > PP_WINDOW_MIN_WIDTH))
{
	show_error("Settings mismatch : Display target size is larget than window minimum size", false);
}